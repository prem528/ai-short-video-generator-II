// The preview (this file) and the server-side MP4 render must use the EXACT
// same composition, otherwise "what you see" won't match "what you export".
// The single source of truth lives in remotion/RemotionComposition.jsx.
export { default } from "../../../remotion/RemotionComposition";
